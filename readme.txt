sortable
========
Sortable table with operations of add, edit, delete row. Use jQuery and PHP without a framework.
Data is saved in MySql.

The database backup is located in the directory docs/
The database configuration can be modified in the file db_persona.php, in the connectDB() function.